[build-system]
requires = [
    "setuptools",
    "setuptools-scm",
    "setuptools-protobuf",
    "numpy"]

build-backend = "setuptools.build_meta"

[tool.setuptools-protobuf]
protobufs = ["proto/epics_event.proto"]

dependencies = [
    "pandas",
    "protobuf >=3.18.3, ==3.*",
    "python-dateutil"
]